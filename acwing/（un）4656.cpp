#include <iostream>
#include <algorithm>
#include <utility>
using namespace std;
const int N = 1e6 + 10;
int n,m;
pair<int,int> p[N];

int main()
{
    cin >> n >> m;
    for (int i = 0; i < n; i ++)
        cin >> p[i].first >> p[i].second;
    while(m --)
    {
        int l = 1, r = 1e6;
        
    }

    return 0;
}